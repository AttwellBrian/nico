import React from 'react';

/**
 * Main application. Top-level routing and configuration should happen here.
 */
const App = (): JSX.Element => (
  <div className="App" style={{ maxHeight: '100vh' }}>
  </div>
);

export default App;
